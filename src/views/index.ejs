
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
</head>
<body>
    <div class="w-full h-screen px-6 md:px-20">
        <nav class="flex items-center justify-between py-8">
            <a href="/" class="text-xl text-blue-600 md:text-2xl font-medium">Khaatabook</a>
            <div class="flex flex-col md:items-center md:flex-row gap-1 md:gap-12 ">
                <a class="text-sm md:text-base font-medium opacity-80" href="/">Home</a>
                <a class="text-sm md:text-base font-medium opacity-80" href="/create">Create new hisab</a>
            </div>
        </nav>

        <div class="w-[320px] mt-8">
            <h2 class="mb-4 text-xl font-medium opacity-80">All Hisab Kitaab</h2>
            <% if(files.length > 0){ %>
                <% files.forEach(function(item){ %>
                    <div class="w-full text-white mb-4 flex items-center justify-between bg-blue-500 px-4 py-2 rounded">
                        <div class="flex items-center gap-6">
                            <p class="font-medium"> <%= item.split(".")[0] %> </p>
                            <a class="w-5 h-5 flex items-center justify-center bg-white rounded-full text-xs text-blue-600" href="/readFile/<%= item %>"> <i class="fa-solid fa-arrow-right"></i></a>
                        </div>
                        <div class="flex items-center gap-6">
                            <a href="/edit/<%= item %>"> <i class="fa-solid fa-pen-to-square"></i> </a>
                            <a href="/delete/<%= item %>" class="w-6 h-6 flex items-center justify-center bg-white rounded-full text-sm text-red-400"> <i class="fa-solid fa-trash"></i> </a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No hisaab yet.</p>
            <% } %>
        </div>
    </div>
</body>
</html>
